import { createClient } from '@supabase/supabase-js'; import * as fs from 'fs'; const env = fs.readFileSync('.env.temp', 'utf8').split('
').filter(Boolean).reduce((acc, line) => { const [k, v] = line.split('='); acc[k] = v; return acc; }, {}); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function main() { const {data, error} = await supabase.from('appointments').select('id, scheduled_at, status, services(name, category), pet_id').order('scheduled_at', {ascending: false}).limit(10); console.log(JSON.stringify(data, null, 2)); } main();
